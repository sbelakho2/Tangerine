let (x, y) = (1, 2)
let (a, b, c) = (3, 4, 5)
let (head, ..tail) = [1,2,3,4]
let (first, second, third) = some_tuple()

let point = (10, 20)
match point
when (x, y) then print(x + y)
when (0, 0) then print("origin")
end

for (key, value) in map do
  print(key + "=" + value)
end