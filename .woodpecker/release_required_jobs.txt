# THE RELEASE-REQUIRED JOB SET (the reviewer's item 35 — Woodpecker port)
#
# This file is the EXACT set of the release-required job names — the
# machine-readable authority the Woodpecker `evidence-gate` workflow
# verifies against. Every entry must be a Woodpecker STEP NAME (the
# `- name: <entry>` step in one of the `.woodpecker/*.yaml` workflows),
# and the aggregate `.woodpecker/gate.yaml` must depend on EXACTLY the
# set of workflows that contain at least one of these steps.
#
# The verifier is scripts/check_ci_required_jobs.sh, run by the
# `evidence-gate` workflow. It asserts, both directions, no subset
# tests:
#
#     set(entries of this file) == set(step names across .woodpecker/)
#     set(workflow files containing a required step) == gate depends_on
#
# A job added to the release gate MUST be added here, and an entry here
# that is not a step in a gate-dependency workflow FAILS the gate (the
# policy cannot drift from the aggregate or from this file).
#
# The set mirrors the former GitHub Actions `release-gate.needs:` list.
# `sast` (CodeQL) is NOT listed: CodeQL's `actions` analysis is
# GitHub-Actions-specific and was dropped with the Actions config; the
# remaining config-security role is covered by the `workflow-lint`
# workflow (`woodpecker-cli lint`). See docs/current/ci.md.
#
# Syntax: one job name per line (`[a-z0-9-]+`), `#` comments and blank
# lines ignored. The verifier parses exactly this format.
abi-platform
allocator
atomic-litmus
bootstrap
cfg-matrix
conformance
cross-compile
crypto-kat
db-integration-mysql
db-integration-postgres
doctests
evidence-gate
gfx-ui
gfx-ui-visual
lint
release-proof
stdlib-e106-sweep
stdlib-integration
stdlib-new-modules
verifier-projection
verifier-stress
workflow-lint
