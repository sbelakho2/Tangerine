enum Operator
  None
  Add
  Sub
end

def main() -> Int
  let op = Operator::Add
  let result = match op
    when Operator::None then 10
    when Operator::Add then 42
    when Operator::Sub then 20
  end
  result
end
