use std::test::{assert_eq, assert_true, catch_panic}

def test_assertions()
  assert_eq(1, 1)
  assert_true(true)
  
  let res = catch_panic(|| assert_eq(1, 2))
  assert_true(res.is_some())
end

def main()
  test_assertions()
  println("tg-test rigor tests passed")
end
