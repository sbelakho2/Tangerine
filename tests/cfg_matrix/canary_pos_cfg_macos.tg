use std::core

# Matrix case (b+): compiled with --target aarch64-apple-darwin the macos
# member of the family EXISTS — the un-gated reference resolves and the
# file checks clean.
@cfg(target_os = "macos")
def cfg_macos_marker() -> Int = 0

def main() -> Int
  cfg_macos_marker()
end
