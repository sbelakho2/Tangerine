use std::io::{print, println}

struct Point
  x: Int
  y: Int
end

impl Point
  def manhattan(&self) -> Int
    self.x + self.y
  end
end

def main() -> Int
  let p = Point { x: 3, y: 4 }
  let m = p.manhattan()
  m
end
