# The return inside the loop: the return edge's cleanup runs the
# loop-created locals' drops before the function exits.
def main() -> Int
  for i in 0..10 do
    let s = "r".to_string() + i.to_string()
    if i == 3 then
      return s.len() + i
    end
  end
  return 0
end
