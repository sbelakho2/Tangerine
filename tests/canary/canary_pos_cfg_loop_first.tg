# The bare `loop` as the FIRST statement — the lexical key vs the loop-join
# collision class; the loop executes once via the break, the continuation
# runs on the loop-join frame.
def main() -> Int
  var total = 0
  loop do
    total = total + 1
    break
  end
  return total
end
