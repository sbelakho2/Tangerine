# Negative shape: read-before-init — a set-convention parameter is read
# before the set-initialization. The oracle (shape neg_read_before_init)
# predicts REJECT; the runner verifies `tg check` rejects this file.
use std::core

resource R
  a: Int
end

def read_before_set(set r: R) -> Int
  r.a
end

def main() -> Int
  0
end
