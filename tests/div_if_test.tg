use std::io::{print, println}

def div_if(a: Int, b: Int) -> Int
  if b == 0 then
    -1
  else
    a / b
  end
end

def main() -> Int
  let r = div_if(84, 12)
  # Exit with result directly so we can check
  r
end
