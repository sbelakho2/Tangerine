# Tangerine ARM64 encoder/ABI test manifest — the authority for tests/arm64/.
#
# Arch-specific native suite for the aarch64 bootstrap host. This suite is a
# mandatory bootstrap acceptance gate on aarch64 hosts: a missing directory,
# missing manifest, unlisted test, missing test or count drift is a hard
# failure (see scripts/bootstrap_helpers.sh bh_require_canary_suites).
#
# count: 2

test_arm64_abi.tg
test_arm64_encoder.tg
