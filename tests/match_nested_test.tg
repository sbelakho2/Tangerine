def apply(op: Int, a: Int, b: Int) -> Int
  match op
  when 1 then a + b
  when 2 then a - b
  when 3 then a * b
  when 4 then
    if b == 0 then -1 else a / b end
  else 0
  end
end

def main() -> Int
  apply(4, 84, 12)
end
