# Negative canary: moving a resource into an array literal transfers
# ownership — using the original afterwards is a use-after-move.
# Expected error: resource checker reports use of a consumed resource
use std::core

resource File
  fd: Int
end

def main() -> Int
  let r = File { fd: 1 }
  let a = [r]
  r.fd
end
