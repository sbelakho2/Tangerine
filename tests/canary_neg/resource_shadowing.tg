use std::core

resource R
  a: Int
end

def make_r() -> R
  R { a: 1 }
end

def consume_r(sink r: R) -> Unit
end

def main() -> Int
  let r = make_r()
  do
    let r = make_r()
    consume_r(r)
  end
  consume_r(r)
  consume_r(r)
  0
end
