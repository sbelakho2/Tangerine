# Negative conformance canary (k): the impl method must not require
# effects beyond the trait contract — the trait declares no effects, the
# impl method declares `effect Io` — rejected (E0230).
use std::core

trait ConvEffects
  def m(self: Self) -> Int
end

struct EffectsImpl
  v: Int
end

impl ConvEffects for EffectsImpl
  def m(self: EffectsImpl) -> Int effect Io
    0
  end
end

def main() -> Int
  0
end
