# Negative purity canary (third-audit item 25): a `pure` function with an
# INDIRECT call (through a function-pointer value) is Unknown — the callee
# set cannot be proven pure, so the annotation fails closed under E0234.
pure def dispatch(cb: fn(Int) -> Int) -> Int
  cb(7)
end

def main() -> Int
  0
end
