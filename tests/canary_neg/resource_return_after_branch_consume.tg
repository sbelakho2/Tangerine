use std::core

resource R
  a: Int
end

def consume_r(sink r: R) -> Unit
end

def maybe_consume(c: Bool) -> R
  let r = R { a: 1 }
  if c then
    consume_r(r)
  else
    ()
  end
  return r
end

def main() -> Int
  maybe_consume(true)
  0
end
