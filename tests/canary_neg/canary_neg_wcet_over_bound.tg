# The WCET hard validity: the annotated body's static worst-case cost
# exceeds the declared bound — rejected.
@[wcet_us(5)]
def heavy() -> Int
  var total = 0
  for i in 0..10 do
    total = total + i
  end
  return total
end
