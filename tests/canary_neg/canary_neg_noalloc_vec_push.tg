# Negative effect-class canary (audit §11): @no_alloc rejects a Vec.push
# — the growing push lowers to the heap-allocating ArrayPush intrinsic
# operation (E0232).
def main() -> Int
  0
end

@no_alloc
def vpush() -> Int
  let v = Vec[Int]::new()
  v.push(1)
  v.push(2)
  0
end
