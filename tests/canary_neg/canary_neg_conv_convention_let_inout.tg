# Negative conformance canary (f2): the access conventions must match the
# trait contract EXACTLY — the INVERSE of the reviewer's case: the trait
# declares `let x: Int`, the impl declares `inout x: Int` — rejected
# (E0225).
use std::core

trait ConvConvention2
  def m(self: Self, x: Int) -> Int
end

struct ConventionImpl2
  v: Int
end

impl ConvConvention2 for ConventionImpl2
  def m(self: ConventionImpl2, inout x: Int) -> Int
    0
  end
end

def main() -> Int
  0
end
