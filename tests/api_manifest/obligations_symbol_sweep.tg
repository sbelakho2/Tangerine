# tests/api_manifest/obligations_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::obligations (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: fail

use std::obligations::{emit_obligation_diagnostic, get_global_registry}

def obligations_sweep_probe() -> Int
  let _f0 = emit_obligation_diagnostic
  let _r1 = get_global_registry()
  0
end
