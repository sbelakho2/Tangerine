# tests/api_manifest/ui_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::ui (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: blit
#   uncovered-callable: bottom
#   uncovered-callable: bytes_per_pixel
#   uncovered-callable: center
#   uncovered-callable: crop
#   uncovered-callable: cyan
#   uncovered-callable: distance
#   uncovered-callable: dot
#   uncovered-callable: draw_circle_points
#   uncovered-callable: draw_image
#   uncovered-callable: draw_image_scaled
#   uncovered-callable: draw_line
#   uncovered-callable: draw_text
#   uncovered-callable: fill_circle
#   uncovered-callable: fill_ellipse
#   uncovered-callable: fill_polygon
#   uncovered-callable: filled
#   uncovered-callable: flip_horizontal
#   uncovered-callable: flip_vertical
#   uncovered-callable: from_hex_str
#   uncovered-callable: from_hsla
#   uncovered-callable: from_image
#   uncovered-callable: from_points
#   uncovered-callable: from_raw
#   uncovered-callable: get_image
#   uncovered-callable: get_image_mut
#   uncovered-callable: get_pixel
#   uncovered-callable: gray
#   uncovered-callable: inset
#   uncovered-callable: intersects
#   uncovered-callable: invert
#   uncovered-callable: magenta
#   uncovered-callable: measure_text
#   uncovered-callable: reset_transform
#   uncovered-callable: rotate
#   uncovered-callable: rotation
#   uncovered-callable: set_fill_color
#   uncovered-callable: set_pixel
#   uncovered-callable: set_stroke_color
#   uncovered-callable: set_stroke_width
#   uncovered-callable: set_transform
#   uncovered-callable: skew
#   uncovered-callable: stroke_circle
#   uncovered-callable: stroke_rect
#   uncovered-callable: to_grayscale
#   uncovered-callable: to_hex
#   uncovered-callable: to_rgba
#   uncovered-callable: transform_point
#   uncovered-callable: with_alpha
#   uncovered-callable: with_easing
#   uncovered-callable: yellow

use std::ui::{lerp_Float, lerp_point}

def ui_sweep_probe() -> Int
  let _f0 = lerp_Float
  let _f1 = lerp_point
  0
end
