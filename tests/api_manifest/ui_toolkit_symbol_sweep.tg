# tests/api_manifest/ui_toolkit_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::ui_toolkit (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: draw_image
#   uncovered-callable: fill_path
#   uncovered-callable: fill_rrect
#   uncovered-callable: pop_clip
#   uncovered-callable: pop_transform
#   uncovered-callable: push_clip_rect
#   uncovered-callable: push_transform
#   uncovered-callable: semantics
#   uncovered-callable: stroke_path
#   uncovered-callable: stroke_rect

use std::ui_toolkit::{_gen_id, _point_in_rect, _slider_update_value, button, canvas, column, grid, image_view, label, row, scroll, separator, slider, spacer, stack, textbox, toggle}

def ui_toolkit_sweep_probe() -> Int
  let _r0 = _gen_id()
  let _f1 = _point_in_rect
  let _f2 = _slider_update_value
  let _f3 = button
  let _f4 = canvas
  let _f5 = column
  let _f6 = grid
  let _f7 = image_view
  let _f8 = label
  let _f9 = row
  let _f10 = scroll
  let _f11 = separator
  let _f12 = slider
  let _f13 = spacer
  let _f14 = stack
  let _f15 = textbox
  let _f16 = toggle
  0
end
