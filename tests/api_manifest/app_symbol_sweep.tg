# tests/api_manifest/app_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::app (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: has_any
#   uncovered-callable: hidpi
#   uncovered-callable: none
#   uncovered-callable: recorder_start
#   uncovered-callable: recorder_stop
#   uncovered-callable: set_title

use std::app::{_log_event, acquire_display_cap, clipboard_cap, clock_cap, drag_drop_cap, fs_cap, ime_cap, random_cap}

def app_sweep_probe() -> Int
  let _f0 = _log_event
  let _r1 = acquire_display_cap()
  let _r2 = clipboard_cap()
  let _r3 = clock_cap()
  let _r4 = drag_drop_cap()
  let _r5 = fs_cap()
  let _r6 = ime_cap()
  let _r7 = random_cap()
  0
end
