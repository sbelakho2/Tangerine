# tests/api_manifest/url_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::url (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: UrlBuilder::new
#   uncovered-callable: consume_char
#   uncovered-callable: consume_str
#   uncovered-callable: find_first_of
#   uncovered-callable: is_secure
#   uncovered-callable: parse_authority
#   uncovered-callable: parse_path
#   uncovered-callable: parse_query
#   uncovered-callable: parse_scheme
#   uncovered-callable: peek
#   uncovered-callable: port_or_default
#   uncovered-callable: query_params
#   uncovered-callable: username

use std::url::{build_query_string, char_to_utf8, hex_digit, hex_value, is_alpha, is_digit, normalize_path, parse_port, parse_query_string, url_error_message}

def url_sweep_probe() -> Int
  let _f0 = build_query_string
  let _f1 = char_to_utf8
  let _f2 = hex_digit
  let _f3 = hex_value
  let _f4 = is_alpha
  let _f5 = is_digit
  let _f6 = normalize_path
  let _f7 = parse_port
  let _f8 = parse_query_string
  let _f9 = url_error_message
  0
end
