# tests/api_manifest/cli_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::cli (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: Matches::new
#   uncovered-callable: ProgressBar::new
#   uncovered-callable: Spinner::new
#   uncovered-callable: author
#   uncovered-callable: default_value
#   uncovered-callable: disable_help
#   uncovered-callable: disable_version
#   uncovered-callable: find_flag
#   uncovered-callable: find_flag_by_short
#   uncovered-callable: find_subcommand
#   uncovered-callable: get_strings
#   uncovered-callable: help
#   uncovered-callable: is_present
#   uncovered-callable: print_help
#   uncovered-callable: print_version
#   uncovered-callable: value_name

use std::cli::{_log_cli_event, cli_error_message}

def cli_sweep_probe() -> Int
  let _f0 = _log_cli_event
  let _f1 = cli_error_message
  0
end
