# tests/api_manifest/linalg_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::linalg (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: angle
#   uncovered-callable: angle_between
#   uncovered-callable: conjugate
#   uncovered-callable: determinant
#   uncovered-callable: distance
#   uncovered-callable: dot
#   uncovered-callable: from_axis_angle
#   uncovered-callable: from_cols
#   uncovered-callable: from_euler
#   uncovered-callable: from_mat3
#   uncovered-callable: from_mat4
#   uncovered-callable: from_rows
#   uncovered-callable: from_simd
#   uncovered-callable: get_scale
#   uncovered-callable: get_translation
#   uncovered-callable: inverse
#   uncovered-callable: length_squared
#   uncovered-callable: lerp
#   uncovered-callable: look_at
#   uncovered-callable: max_component
#   uncovered-callable: min_component
#   uncovered-callable: mul_component
#   uncovered-callable: mul_mat2
#   uncovered-callable: mul_mat3
#   uncovered-callable: mul_mat4
#   uncovered-callable: mul_vec2
#   uncovered-callable: mul_vec3
#   uncovered-callable: mul_vec4
#   uncovered-callable: ortho
#   uncovered-callable: perpendicular
#   uncovered-callable: perspective
#   uncovered-callable: perspective_gl
#   uncovered-callable: project_onto
#   uncovered-callable: reflect
#   uncovered-callable: rotate
#   uncovered-callable: rotate_vec3
#   uncovered-callable: rotation
#   uncovered-callable: rotation_axis
#   uncovered-callable: rotation_x
#   uncovered-callable: rotation_y
#   uncovered-callable: rotation_z
#   uncovered-callable: scaling
#   uncovered-callable: slerp
#   uncovered-callable: splat
#   uncovered-callable: to_axis_angle
#   uncovered-callable: to_mat3
#   uncovered-callable: to_mat4
#   uncovered-callable: to_simd
#   uncovered-callable: to_vec3
#   uncovered-callable: to_vec4
#   uncovered-callable: transform_direction
#   uncovered-callable: transform_point
#   uncovered-callable: transpose
#   uncovered-callable: xy
#   uncovered-callable: xyz

use std::linalg::{test_mat4_identity_mul, test_mat4_inverse, test_perspective_projection, test_quat_rotation, test_quat_slerp, test_transform_composition, test_vec3_cross_product}

def linalg_sweep_probe() -> Int
  let _r0 = test_mat4_identity_mul()
  let _r1 = test_mat4_inverse()
  let _r2 = test_perspective_projection()
  let _r3 = test_quat_rotation()
  let _r4 = test_quat_slerp()
  let _r5 = test_transform_composition()
  let _r6 = test_vec3_cross_product()
  0
end
