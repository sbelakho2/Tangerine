# tests/api_manifest/ctx_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::ctx (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: message

use std::ctx::{__intrinsic_ctx_optimize_selection, __intrinsic_lookup_symbol_metadata, _expand_dependencies, _lookup_symbol_metadata, get_pack, register_pack}

def ctx_sweep_probe() -> Int
  let _f0 = __intrinsic_ctx_optimize_selection
  let _f1 = __intrinsic_lookup_symbol_metadata
  let _f2 = _expand_dependencies
  let _f3 = _lookup_symbol_metadata
  let _f4 = get_pack
  let _f5 = register_pack
  0
end
