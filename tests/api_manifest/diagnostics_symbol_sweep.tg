# tests/api_manifest/diagnostics_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::diagnostics (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: capability_list
#   uncovered-callable: dump
#   uncovered-callable: enable_all
#   uncovered-callable: formatted
#   uncovered-callable: header
#   uncovered-callable: size_bytes

use std::diagnostics::{dump_ui_tree}

def diagnostics_sweep_probe() -> Int
  let _f0 = dump_ui_tree
  0
end
