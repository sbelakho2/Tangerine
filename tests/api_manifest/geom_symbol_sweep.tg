# tests/api_manifest/geom_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::geom (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: bottom
#   uncovered-callable: center
#   uncovered-callable: dot
#   uncovered-callable: from_rect
#   uncovered-callable: from_srgb8
#   uncovered-callable: from_xywh
#   uncovered-callable: to_srgb8
#   uncovered-callable: with_alpha

use std::geom::{_path_data, circle_contains, circle_intersects, circle_rect_intersects, path_bounds, path_close, path_cubic_to, path_line_to, path_move_to, path_quad_to, ray_circle_intersect, ray_rect_intersect, rect_contains, segment_closest_point, segment_distance, segments_intersect, transform_mul, transform_rotate}

def geom_sweep_probe() -> Int
  let _f0 = _path_data
  let _f1 = circle_contains
  let _f2 = circle_intersects
  let _f3 = circle_rect_intersects
  let _f4 = path_bounds
  let _f5 = path_close
  let _f6 = path_cubic_to
  let _f7 = path_line_to
  let _f8 = path_move_to
  let _f9 = path_quad_to
  let _f10 = ray_circle_intersect
  let _f11 = ray_rect_intersect
  let _f12 = rect_contains
  let _f13 = segment_closest_point
  let _f14 = segment_distance
  let _f15 = segments_intersect
  let _f16 = transform_mul
  let _f17 = transform_rotate
  0
end
