# tests/api_manifest/supply_chain_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::supply_chain (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.

use std::supply_chain::{build_trust_graph, check_policy, clamp_float, compute_build_id, compute_trust_score, count_transitive_deps, count_transitive_deps_inner, default_policy, default_reproducible_config, lockfile_parse, lockfile_serialize, to_float, verify_reproducible}

def supply_chain_sweep_probe() -> Int
  let _f0 = build_trust_graph
  let _f1 = check_policy
  let _f2 = clamp_float
  let _f3 = compute_build_id
  let _f4 = compute_trust_score
  let _f5 = count_transitive_deps
  let _f6 = count_transitive_deps_inner
  let _r7 = default_policy()
  let _r8 = default_reproducible_config()
  let _f9 = lockfile_parse
  let _f10 = lockfile_serialize
  let _f11 = to_float
  let _f12 = verify_reproducible
  0
end
