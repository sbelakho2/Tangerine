# tests/api_manifest/float_control_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::float_control (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: none

use std::float_control::{approx_eq_ulp_f32, approx_eq_ulp_f64, check_exceptions, clear_exception_flags, deterministic_float, fast_math, get_denormal_mode, get_exception_flags, get_rounding_mode, next_after_f32, next_after_f64, set_denormal_mode, set_rounding_mode, ulp_distance_f32, ulp_distance_f64}

def float_control_sweep_probe() -> Int
  let _f0 = approx_eq_ulp_f32
  let _f1 = approx_eq_ulp_f64
  let _f2 = check_exceptions
  let _f3 = clear_exception_flags
  let _f4 = deterministic_float
  let _f5 = fast_math
  let _f6 = get_denormal_mode
  let _f7 = get_exception_flags
  let _f8 = get_rounding_mode
  let _f9 = next_after_f32
  let _f10 = next_after_f64
  let _f11 = set_denormal_mode
  let _f12 = set_rounding_mode
  let _f13 = ulp_distance_f32
  let _f14 = ulp_distance_f64
  0
end
