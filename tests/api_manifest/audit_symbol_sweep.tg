# tests/api_manifest/audit_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::audit (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.

use std::audit::{_audit_id, _get_symbol_location, _has_capability_drift, _has_stub_patterns, _scan_scope_symbols}

def audit_sweep_probe() -> Int
  let _f0 = _audit_id
  let _f1 = _get_symbol_location
  let _f2 = _has_capability_drift
  let _f3 = _has_stub_patterns
  let _f4 = _scan_scope_symbols
  0
end
