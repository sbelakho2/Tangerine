# tests/api_manifest/contracts_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::contracts (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: code
#   uncovered-callable: message

use std::contracts::{check_guard, check_invariant, check_post, format_obligation, guard_else_diverges, is_narrowing_condition, kind_name, make_contract, make_invariant, make_obligation, make_post, make_pre, mark_verified, with_message}

def contracts_sweep_probe() -> Int
  let _f0 = check_guard
  let _f1 = check_invariant
  let _f2 = check_post
  let _f3 = format_obligation
  let _f4 = guard_else_diverges
  let _f5 = is_narrowing_condition
  let _f6 = kind_name
  let _f7 = make_contract
  let _f8 = make_invariant
  let _f9 = make_obligation
  let _f10 = make_post
  let _f11 = make_pre
  let _f12 = mark_verified
  let _f13 = with_message
  0
end
