# tests/api_manifest/toml_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::toml (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: TomlStringifier::new
#   uncovered-callable: ensure_table_path
#   uncovered-callable: expect_char
#   uncovered-callable: insert_value
#   uncovered-callable: insert_value_path
#   uncovered-callable: parse_array
#   uncovered-callable: parse_bare_key
#   uncovered-callable: parse_basic_string
#   uncovered-callable: parse_bool
#   uncovered-callable: parse_date_part
#   uncovered-callable: parse_datetime_value
#   uncovered-callable: parse_float_value
#   uncovered-callable: parse_inline_table
#   uncovered-callable: parse_int_value
#   uncovered-callable: parse_key
#   uncovered-callable: parse_key_path
#   uncovered-callable: parse_literal_string
#   uncovered-callable: parse_number_or_datetime
#   uncovered-callable: parse_offset
#   uncovered-callable: parse_time_part
#   uncovered-callable: parse_time_with_offset
#   uncovered-callable: parse_unicode_escape
#   uncovered-callable: parse_value
#   uncovered-callable: peek
#   uncovered-callable: peek_n
#   uncovered-callable: skip_ws
#   uncovered-callable: skip_ws_and_newlines
#   uncovered-callable: write_datetime
#   uncovered-callable: write_inline_value
#   uncovered-callable: write_key
#   uncovered-callable: write_string
#   uncovered-callable: write_table
#   uncovered-callable: write_value

use std::toml::{datetime_to_string, format_hex4, format_int_pad, from_toml, is_bare_key_char, parse_int_simple, parse_nanoseconds, to_toml, toml_error_message, toml_from_value, toml_to_value, toml_value_as_array, toml_value_as_float, toml_value_as_table}

def toml_sweep_probe() -> Int
  let _f0 = datetime_to_string
  let _f1 = format_hex4
  let _f2 = format_int_pad
  let _f3 = from_toml
  let _f4 = is_bare_key_char
  let _f5 = parse_int_simple
  let _f6 = parse_nanoseconds
  let _f7 = to_toml
  let _f8 = toml_error_message
  let _f9 = toml_from_value
  let _f10 = toml_to_value
  let _f11 = toml_value_as_array
  let _f12 = toml_value_as_float
  let _f13 = toml_value_as_table
  0
end
