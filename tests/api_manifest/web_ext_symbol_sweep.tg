# tests/api_manifest/web_ext_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::web_ext (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: cancel
#   uncovered-callable: check_fixed_window
#   uncovered-callable: check_token_bucket
#   uncovered-callable: connection_closed
#   uncovered-callable: connection_opened
#   uncovered-callable: default
#   uncovered-callable: extract_key
#   uncovered-callable: initiate
#   uncovered-callable: is_done
#   uncovered-callable: is_file
#   uncovered-callable: is_shutting_down
#   uncovered-callable: on_shutdown
#   uncovered-callable: permissive
#   uncovered-callable: save_to
#   uncovered-callable: shutdown
#   uncovered-callable: status
#   uncovered-callable: submit
#   uncovered-callable: text
#   uncovered-callable: with_header
#   uncovered-callable: with_retry_after

use std::web_ext::{add_health_checks, handle_health_checks, handle_health_live, handle_health_ready, install_signal_handler, parse_multipart, task_pool_worker, task_status_read, task_status_write}

def web_ext_sweep_probe() -> Int
  let _f0 = add_health_checks
  let _f1 = handle_health_checks
  let _f2 = handle_health_live
  let _f3 = handle_health_ready
  let _f4 = install_signal_handler
  let _f5 = parse_multipart
  let _f6 = task_pool_worker
  let _f7 = task_status_read
  let _f8 = task_status_write
  0
end
