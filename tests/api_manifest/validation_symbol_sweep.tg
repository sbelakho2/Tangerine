# tests/api_manifest/validation_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::validation (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: _add
#   uncovered-callable: clone
#   uncovered-callable: email
#   uncovered-callable: fmt
#   uncovered-callable: for_field
#   uncovered-callable: integer
#   uncovered-callable: into_result
#   uncovered-callable: is_empty
#   uncovered-callable: len
#   uncovered-callable: max_length
#   uncovered-callable: merge
#   uncovered-callable: min_length
#   uncovered-callable: nest
#   uncovered-callable: one_of
#   uncovered-callable: pattern
#   uncovered-callable: string
#   uncovered-callable: to_json
#   uncovered-callable: url
#   uncovered-callable: validate
#   uncovered-callable: with_param

use std::validation::{string_field, validated}

def validation_sweep_probe() -> Int
  let _f0 = string_field
  let _f1 = validated
  0
end
