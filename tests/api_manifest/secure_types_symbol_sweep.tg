# tests/api_manifest/secure_types_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::secure_types (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.

use std::secure_types::{contains_injection_risk, count_placeholders, escape_sql_string, header_new, html_concat, html_safe_trusted, path_extension, safe_url_parse, safe_url_to_string, split_url, sql_debug_string}

def secure_types_sweep_probe() -> Int
  let _f0 = contains_injection_risk
  let _f1 = count_placeholders
  let _f2 = escape_sql_string
  let _f3 = header_new
  let _f4 = html_concat
  let _f5 = html_safe_trusted
  let _f6 = path_extension
  let _f7 = safe_url_parse
  let _f8 = safe_url_to_string
  let _f9 = split_url
  let _f10 = sql_debug_string
  0
end
