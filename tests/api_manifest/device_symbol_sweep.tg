# tests/api_manifest/device_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::device (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.
#
# HONEST REMAINING GAP (methods/constructors — not mechanically
# generatable as code references):
#   uncovered-callable: code
#   uncovered-callable: message

use std::device::{_device_class_eq, _discover_cpu, _discover_gpus, _log_device_event, _parse_hex, select_device, set_global_logger}

def device_sweep_probe() -> Int
  let _f0 = _device_class_eq
  let _r1 = _discover_cpu()
  let _r2 = _discover_gpus()
  let _f3 = _log_device_event
  let _f4 = _parse_hex
  let _f5 = select_device
  let _f6 = set_global_logger
  0
end
