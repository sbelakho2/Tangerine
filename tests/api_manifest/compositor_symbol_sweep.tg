# tests/api_manifest/compositor_symbol_sweep.tg — the per-symbol coverage sweep
# (generated by scripts/gen_api_manifest_sweep.py — do not edit;
#  the gate regenerates and diffs).
#
# The uncovered callables of std::compositor (the manifest's per-symbol
# enumeration, bounded to the behavior families) are referenced
# in REAL code below: use-imported, bound as function values, and
# called when zero-argument.

use std::compositor::{_log_compositor_event}

def compositor_sweep_probe() -> Int
  let _f0 = _log_compositor_event
  0
end
