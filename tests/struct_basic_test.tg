use std::io::{print, println}

struct Point
  x: Int
  y: Int
end

def main() -> Int
  let p = Point { x: 3, y: 4 }
  let px = p.x
  let py = p.y
  # Use exit code to verify field access works
  if px == 3 && py == 4 then
    42
  else
    1
  end
end
