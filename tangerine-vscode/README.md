# Tangerine Language (VS Code)

This extension provides basic syntax highlighting for Tangerine files (`.tg`).

## Local use

1. Open this folder in VS Code.
2. Press F5 to launch an Extension Development Host.
3. Open a `.tg` file and verify highlighting.

## Publish (optional)

Use `vsce package` to build a .vsix for installation.
