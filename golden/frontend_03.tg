## Golden test: frontend milestone 3 — traits, impls, method sigs
# Tests: trait declarations, impl blocks, for clause, method bodies

trait Display
  def display(self) -> String
end

trait Debug
  def debug(self) -> String
end

struct Name
  value: String
end

impl Display for Name
  def display(self) -> String
    self.value
  end
end
