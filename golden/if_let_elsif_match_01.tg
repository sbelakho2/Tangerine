use std::core::{Option, Result}

def unwrap_or_zero(value: Option[Int]) -> Int
  if let Option::Some(v) = value then
    v
  elsif let Result::Ok(other) = Result::Ok(7) then
    other
  else
    0
  end
end