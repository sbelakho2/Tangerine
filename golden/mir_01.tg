## Golden test: MIR — basic lowering, CFG, branches

def fib(n: Int) -> Int
  if n <= 1
    n
  else
    fib(n - 1) + fib(n - 2)
  end
end

def factorial(n: Int) -> Int
  mut result: Int = 1
  mut i: Int = 1
  while i <= n
    result = result * i
    i = i + 1
  end
  result
end
