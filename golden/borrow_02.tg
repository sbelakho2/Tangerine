## Golden test: access checker — conflicting inout accesses rejected
# expect: error

def adjust(inout a: Int, inout b: Int) -> Int
  a + b
end

def double_inout_access() -> Int
  let mut v: Int = 0
  adjust(&v, &v)
end
