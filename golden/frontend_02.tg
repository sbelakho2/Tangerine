# Golden test: frontend milestone 2 — structs, enums, fields
# Tests: struct declarations, enum declarations, field types, generics

struct Point
  x: Int
  y: Int
end

struct Pair[T]
  first: T
  second: T
end

enum Color
  Red
  Green
  Blue
end

enum Option[T]
  Some(T)
  None
end
