# Tangerine Feature Registry

> **GENERATED EVIDENCE — do not edit by hand.** This document is
> rendered by `scripts/gen_feature_registry.sh` from
> [`features.toml`](../../features.toml), the machine-readable source of
> truth: the feature ID, the status, the supported targets, and the test
> references. The CI evidence-gate job regenerates it and runs
> `git diff --exit-code`, so a hand-edited or drifted registry cannot
> merge. Every feature appears exactly once with exactly one status;
> the statuses are **mechanically derived** (test-file existence + suite
> membership + the declared CI gates), never hand-asserted, and the
> item-36 **status ladder** is **calculated**, never typed.

## How a status is decided

The generator verifies each row's evidence before it renders:

- every **test reference** must exist in the tree, and every named
  **suite manifest** (`tests/canary/MANIFEST`, `tests/canary_neg/MANIFEST`,
  `tests/arm64/MANIFEST`) must contain the listed file — a listed artifact
  that does not exist fails the generation;
- every named **CI gate** must be a job in `.github/workflows/ci.yml`;
- `implemented` derives to **implemented + test-covered** (committed test
  artifacts exist) or **implemented + unverified** (no committed test
  artifact), never to run-verified.

## The status ladder (the reviewer's item 36)

The ladder position is CALCULATED from the committed evidence (the
typed `status` in features.toml is only the DECLARED input):

| Position | Derivation (what evidence must exist) |
|----------|----------------------------------------|
| `DECLARED` | the row exists in features.toml |
| `IMPLEMENTED` | the declared status is implemented/partial AND every enforcing-code file/dir reference exists in the tree |
| `SEMANTICALLY_CHECKED` | every listed test artifact exists (committed; std modules additionally parse-clean under the E106 sweep) |
| `NATIVE_TESTED` | at least one listed artifact is a native behavior artifact (a `tests/**/*.tg` canary/@test file or a `tests/run_*.sh` gate) |
| `ADVERSARIAL_TESTED` | at least one listed artifact is adversarial (canary_neg / fuzz / differential / slice_safety / *neg* / *litmus*) |
| `TARGET_COMPLETE` | every declared target is a SERVED target (aarch64-apple-darwin, x86_64-unknown-linux-gnu, x86_64-apple-darwin — windows/wasm32 are NOT served) AND a native artifact exists |
| `EXACT_SHA_VERIFIED` | run evidence at a named SHA: `build/release_evidence.json` (written ONLY by `scripts/gen_status.sh --release-evidence`) carries the tested sha + artifact hashes |
| `RELEASE_GATED` | the feature is listed in the release-evidence file's `release_gated_features` — **never derivable from source**, only from the release-run evidence |

The ladder is a monotone chain: a feature at position N holds every
position below N. The top two positions are RUN facts; this
snapshot records source-state facts only, so nothing above
`TARGET_COMPLETE` appears here unless the release-evidence file is
present in the tree.

> **Execution honesty — the CI-artifact evidence model.** A committed
> test artifact is a source-state fact; an observed execution is a run
> fact, and they are separate statuses:
>
> - **implemented + test-covered** — the repo ships a committed test
>   artifact that exercises the feature (a manifest-registered canary, an
>   invariant group, a required gate, an `@test` file, or the self-host
>   compiler kernel itself as the workload). It does NOT claim the
>   artifact has run anywhere.
> - **implemented + run-verified at SHA** — a CI artifact proves the
>   execution at the named commit: the CI pipeline runs the harness and
>   the suites and uploads the tested-SHA snapshot — the
>   `status-snapshot` CI artifact (STATUS.txt generated by
>   `scripts/gen_status.sh` from the tested commit + the test-artifact
>   hashes). **No feature holds this status in this snapshot** — the
>   ladder/CI has not run on the tested tree, so there is no observed
>   successful execution at a named SHA.
>
> This registry records only source-state facts, never run results.

## Legend

| Status | Meaning |
|--------|---------|
| **implemented + run-verified at SHA** | The compiler/runtime implements the feature AND an observed successful execution of its test artifact at the named commit is proven by a CI artifact (the `status-snapshot` generated by `scripts/gen_status.sh` from the tested SHA). **None in this snapshot** — no ladder/CI run has occurred on the tested tree |
| **implemented + test-covered** | The compiler/runtime implements the feature and a committed test artifact exists (manifest-registered canary, invariant group, required CI gate, `@test` file, or the self-host kernel as workload) that exercises it — but no observed execution at this SHA |
| **implemented + unverified** | The compiler/runtime implements the feature; no committed test artifact exercises it |
| **partial** | A working subset exists; the full feature is not present |
| **API-only** | The std module / interface is declared, but the compiler does not wire it into the pipeline and it is not in the bootstrap closure |

Non-implemented rows keep their unchanged statuses (outside the
implemented-feature vocabulary): **design** — specified in docs/RFCs
only, no implementation; **unsupported** — explicitly removed or
rejected by the compiler (E100/E106 hard errors).

## The experimental gate (the reviewer's item 33)

A row marked **experimental** is a platform-only surface whose targets
are unsupported/API-only (the stable-subset policy): the feature and
its std modules (the row's `modules = [...]`) carry parse-clean
evidence only and are **explicitly excluded from the shipped-std
behavior claims** — see the [Stdlib Completeness Model](stdlib_completeness.md)
and the contracts manifest (`docs/current/stdlib_contracts.toml`). The
generator cross-checks the two registries: every experimental module
must be listed in exactly one experimental row, and every module a
row lists must be experimental in the contracts manifest.

## Registry

| Feature | Status | Ladder | Targets | Enforcing code | Test / assertion | CI gate |
|---------|--------|--------|---------|----------------|------------------|---------|
| `aarch64-target` — aarch64-apple-darwin target (host) — the LSE-required target feature table | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | aarch64 emitter in tg_compiler/asm.tg / codegen.tg; asm.tg requires_lse | tests/arm64/test_arm64_abi.tg; tests/arm64/test_arm64_encoder.tg; tests/pthread_abi_test.tg; tests/syscall_translation_test.tg; tests/target_lse_contract_test.tg; tests/net_negative_abi_test.tg; tests/run_target_lane_canaries.sh | `abi-platform` |
| `access-conventions` — Access conventions (let / inout / sink / set; let is the default observe convention — read-only access, no move; the by-value move is sink) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_param (the ONLY parameter spellings); tg_compiler/types.tg access check | tests/canary/canary_access_read.tg; tests/canary/canary_access_inout.tg; tests/canary/canary_access_set.tg; tests/canary/canary_access_sink.tg; tests/canary_neg/canary_neg_access_sink_twice.tg; tests/canary_neg/canary_neg_access_sink_use_after.tg | — |
| `access-markers` — &place / &mut place call-argument access markers (ExprAccess; the marker is only valid as a call argument) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_unary (ExprAccess); tg_compiler/types.tg rejects the marker outside a call argument list | tests/canary_neg/access_marker_aggregate.tg; tests/canary_neg/access_marker_capture.tg; tests/canary_neg/access_marker_let.tg; tests/canary_neg/access_marker_return.tg | — |
| `adts-pattern-matching` — ADTs + pattern matching (by-value binders only) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg enum / match | tests/canary_neg/canary_neg_ref_pattern.tg | — |
| `algebraic-effects` — Algebraic effects — the typed row: the effect declarations' operations register as the semantic EffectIds' typed op tables (parse_effect_decl op signatures; the checker's TypedEffectDecl records); the std::effects::perform(EFFECT.op, ...) operation-call surface types against the declared ops; the MIR lowers perform to MirEffectPerform (effect id + op index + payload/result frame pointers) and the handler blocks to MirEffectFrameEnter/Exit + the synthesized dispatcher functions; the runtime is the DIRECT-STYLE handler stack (the task's linked handler frames written into the installing function's frame slot) — perform dispatches to the innermost matching handler, a handler that does not handle an op re-performs it to the outer handler (the frame bitmap skip), and an unhandled op is the defined error (stderr message + abort). Retained experimental: the bounded row is Copy-payload only (no resource ownership across the perform boundary) and a handler cannot span an async task boundary. | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/types.tg TypedEffectDecl/typed_effects + the perform/handle typing; tg_compiler/mir.tg MirEffectPerform + MirEffectFrameEnter/Exit + lower_pending_dispatchers; tg_compiler/codegen.tg the __tg_effect_perform / __tg_effect_frame_push / __tg_effect_frame_pop arms + the effect frame slot; tg_compiler/runtime.tg the handler-stack emitters; std/effects.tg perform | tests/effects/effect_state_test.tg; tests/effects/effect_reperform_test.tg; tests/effects/effect_unhandled_test.tg; tests/effects/effect_async_test.tg | — |
| `allocator` — Allocator: per-class free lists + per-size-class locks + 16-byte alignment contract | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/codegen.tg emit_tg_alloc_lock / emit_tg_alloc_unlock; tg_compiler/runtime.tg _tg_mem_alloc / _tg_mem_free / _tg_alloc_free_heads | tests/allocator_churn_test.tg; tests/allocator_large_test.tg; tests/allocator_oom_test.tg; tests/allocator_reuse_test.tg; tests/allocator_threaded_test.tg; tests/allocator_alignment_test.tg | `allocator` |
| `async-await` — Async/await — cooperative single-threaded executor, wakers, timers, the I/O reactor, the deterministic scheduler mode (the module carries the alpha maturity label; the surface is implemented and native-tested: task scopes, cancellation tokens, reactor readiness, async mutex/channel/semaphore waiters, join cancellation, executor clock, db pool) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/async.tg (executor, wakers, timers, the I/O reactor, deterministic mode); executor outside the bootstrap closure | tests/task_scope_test.tg; tests/cancellation_token_test.tg; tests/reactor_readiness_test.tg; tests/async_mutex_waiter_test.tg; tests/join_cancel_test.tg; tests/executor_clock_test.tg; tests/db_async_pool_test.tg | `stdlib-integration` |
| `atomics` — Atomics — the __intrinsic_atomic_* family is the ONE atomic authority (1/2/4/8-byte widths, both arches) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/codegen.tg emit_atomic_intrinsic (inline LDAR/STLR/LSE on AArch64, LOCKed xchg/xadd/cmpxchg on x86-64); std/atomic.tg + std/sync.tg route every op through the family | tests/atomic_litmus_sb_test.tg; tests/atomic_litmus_mp_test.tg; tests/atomic_litmus_wrc_test.tg; tests/atomic_litmus_acqrel_test.tg; tests/atomic_litmus_cas_pub_test.tg; tests/atomic_litmus_cas_fail_test.tg | `atomic-litmus` |
| `bench-framework` — Benchmark framework (std/bench) — in the bootstrap closure; the tg bench outcome-integrity gate is CI-required (stdlib-new-modules) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/bench.tg (in bootstrap/compiler_kernel.manifest); tests/run_bench_runner_integrity.sh covers the seven tg bench outcomes | tests/run_bench_runner_integrity.sh | `stdlib-new-modules` |
| `bootstrap-closure` — Bootstrap closure (14 std + 23 compiler sources) + the stage0->stage3 ladder (stage2 == stage3 byte-identical) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin | bootstrap/compiler_kernel.manifest; run_bootstrap.sh ladder (stage0->stage3); scripts/bootstrap_helpers.sh four-way parity | scripts/bootstrap_helpers.sh | — |
| `budgets` — Budgets (@budget annotations — alloc + time metrics enforced at runtime; the counter table is data-section-defined) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/types.tg TypedBudget; tg_compiler/mir.tg MirBudgetConsume lowering; tg_compiler/codegen.tg __tg_budget_alloc / __tg_budget_time arms; tg_compiler/runtime.tg __tg_clock_ns | tests/canary/canary_pos_budget_alloc_within.tg; tests/canary/canary_pos_budget_alloc_two_calls.tg; tests/canary/canary_pos_budget_alloc_branch_within.tg; tests/canary/canary_pos_budget_nested.tg | — |
| `canary-self-description` — The canary suites' self-describing manifests (generated suite count + manifest sha256 + test-list sha256; the CI generate-then-diff gate — a stale committed count cannot merge) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | scripts/gen_status.sh --refresh-manifests (regenerates the tests/canary, tests/canary_neg, tests/arm64 MANIFEST count/hash lines); scripts/bootstrap_helpers.sh bh_canary_suite_validate | tests/canary/MANIFEST; tests/canary_neg/MANIFEST; tests/arm64/MANIFEST | — |
| `capabilities` — Capabilities — unconditional linearity enforcement | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/resource_check.tg capability machinery (consumption tracking, contains_capability via tg_compiler/types.tg type_properties_of) | tests/canary/canary_capability.tg | — |
| `cfg-elimination` — @cfg target-conditioned declaration elimination (final — an eliminated item never resurrects; empty @cfg() fails closed with E108) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/compiler_core.tg apply_cfg_elimination; tg_compiler/parser.tg E108InvalidCfgPredicate | tests/cfg_matrix/canary_pos_cfg_linux.tg; tests/cfg_matrix/canary_neg_cfg_macos_symbol_on_linux.tg; tests/cfg_matrix/canary_pos_cfg_macos.tg; tests/cfg_matrix/canary_neg_cfg_linux_symbol_on_macos.tg; tests/cfg_matrix/canary_neg_cfg_ungated_windows_ref.tg; tests/cfg_matrix/canary_pos_cfg_host_family.tg; tests/run_cfg_matrix_tests.sh | `cfg-matrix` |
| `closures` — Closures (value-ABI lowering + self-host validation) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | closure lowering in tg_compiler/mir.tg / tg_compiler/codegen.tg; self-host closure validation | tests/canary/canary_closure_async.tg; tests/canary/canary_closure_resource_capture.tg; tests/canary/canary_pos_closure_capture_owner.tg | — |
| `code-formatting` — Code formatting (tg fmt) | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/formatter.tg | — | — |
| `contracts` — Contracts (pre / post / invariant — unconditional runtime trap, never flag-gated) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mir.tg lower_contract -> MirContractCheck; tg_compiler/codegen.tg trap emission | tests/run_mode_behavior_tests.sh | — |
| `cqs` — CQS — the compiler-query-server: the typed-info / symbols / diagnostics queries are INVOKED by the canonical pipeline (the driver's check path + the library API run the queries on the analyzed program + the lowered MIR), and the query results AFFECT the compilation where required (the mode matrix's gated failures are reported and fail the check) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/cqs.tg cqs_query_server_new / cqs_run_queries (the three queries: cqs_query_symbols, cqs_query_typed_info, cqs_query_diagnostics) + the FULL detector wiring in run_cqs_analysis; tg_compiler/driver.tg driver_cqs_query_pass (the check path's invocation); tg_compiler/lib.tg analyze_source_cqs (the library pipeline's invocation) | tests/cqs_pipeline_test.tg | — |
| `crypto-kat` — Crypto KAT suite (known-answer vectors over std/crypto.tg primitives) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | tests/unit/test_crypto_rigor.tg (sha256, hmac, aes, hex, base64, ...) | tests/unit/test_crypto_rigor.tg | `crypto-kat` |
| `db-drivers` — std::db driver hardening (Postgres ? -> $n translation + PgParamBacking; MySQL real MYSQL_BIND binding; lifecycle/pool/layout suites) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/db.tg postgres_translate_placeholders / PgParamBacking / PQexecPrepared / MysqlBind / mysql_stmt_bind_param; native/mysql_shims.c | tests/db_postgres_integration_test.tg; tests/db_mysql_integration_test.tg; tests/db_lifecycle_test.tg; tests/db_async_pool_test.tg; tests/db_postgres_lexer_test.tg; tests/db_mysql_layout_probe_test.tg; tests/db_mysql_large_result_test.tg | `db-integration-postgres` |
| `docgen` — Documentation generator (tg doc) — no test evidence | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/docgen.tg | — | — |
| `doctests` — Documentation doctests (docs/current/** coverage; compile_fail: <expected diagnostic> convention; the history excluded) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | scripts/check_doctests.sh (the seven canonical documents compiled/checked; every docs/current document coverage-scanned; docs/history excluded) | scripts/check_doctests.sh | `doctests` |
| `drop-glue` — Drop glue (per-concrete-plan, recursion broken by symbol; DeinitPlan::String -> _tg_string_drop) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mir.tg mir_build_all_drop_glues; tg_compiler/runtime.tg _tg_string_drop | tests/canary/canary_pos_container_destroy.tg | — |
| `embedded-targets` — Embedded targets — THE ISA CODE-GEN TRUTH (P0.2): aarch64-unknown-none (ARM64 bare-metal) is the ONLY embedded triple with real code generation — the TARGET SPEC (arch/cpu/fpu/endianness/pointer width/max atomic width/linker/abort-only panic) emitted as the machine-readable JSON artifact, the LINKER SCRIPT (the flash/RAM MEMORY layout + the .isr_vector/.text/.rodata/.data/.bss section placement + the __data_load copy table + the stack top), the STARTUP (the reset handler contract — the .bss zeroing + the .data copy + the user main call — plus the interrupt vector table), the @interrupt SIGNATURE GATE (an ISR takes no parameters) + the vector-table collection, the VOLATILE/MMIO load/store intrinsics (the runtime's _tg_volatile_read*/_write* arms — the width-exact aarch64 ldr[b\|h]/str[b\|h] + x86-64 movzx/mov bodies the optimizer cannot elide), the atomicity availability (64), the allocator-free core-only surface (the no_std allowlist), the section placement (@link_section), the PANIC/ABORT contract, and the BARE-METAL aarch64 ELF image (the reset-vector entry, the ELF magic + non-zero entry structurally verified) produced by the REAL aarch64 backend; the Thumb (thumbv6m-none-eabi / thumbv7em-none-eabi[f] / thumbv8m.main-none-eabihf) and RISC-V (riscv32imc\|imac-unknown-none-elf / riscv64gc-unknown-none-elf) triples are HARD-REJECTED — no Thumb/RISC-V code generator exists, so the route emits the stable rejection diagnostic and NO artifact (it never fabricates an aarch64 image under a foreign triple); there is NO QEMU execution lane — the old print-only lane was removed (hardware execution is not claimed) | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/driver.tg compile_to_embedded_route (the ISA-CODEGEN gate — the family check that HARD-REJECTS the Thumb/RISC-V triples before any artifact; embedded_spec_for / embedded_aarch64_linker_script / embedded_cortex_m_linker_script / embedded_riscv_linker_script / embedded_spec_json / embedded_startup_source / collect_interrupt_fns / collect_link_sections / embedded_elf_entry_ok); tg_compiler/runtime.tg emit_volatile_runtime (the _tg_volatile_read*/_write* load/store arms); tg_compiler/linker.tg linker_resolve_entry_name (the _reset_handler fallback); std/embedded.tg (the Register/ReadOnly/WriteOnly MMIO abstraction, the @interrupt/@link_section/@compiler_attribute markers, the interrupt_vector_table, target_max_atomic_width, the linker script templates) | tests/embedded/embedded_blinky.tg; tests/embedded/embedded_mmio_behavior_test.tg; tests/run_embedded_lane.sh | — |
| `error-handling` — Error handling (Result[T, E] / Option[T], ?, try/catch) + the E0xxx diagnostic registry | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | std/core.tg Result / Option; tg_compiler/parser.tg error_code_to_string | tests/canary/canary_pos_resource_option_result.tg | — |
| `exec-thread-pool` — std/exec work-stealing thread pool with the conservation ledger | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/exec.tg Executor (submit / submit_with_result / shutdown / conservation_ok) | tests/exec_conservation_test.tg | — |
| `extern-abi-reference-exception` — Extern-ABI reference exception — __intrinsic_*-named externs only | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_extern_fn / parse_extern_static (extern_abi_context = is_intrinsic_extern_name); tg_compiler/ids.tg is_intrinsic_extern_name (the __intrinsic_ prefix) | tests/run_stdlib_e106_sweep.sh | `stdlib-e106-sweep` |
| `feature-registry-evidence` — The generated feature registry (features.toml -> feature_registry.md; statuses mechanically derived; CI generate-then-diff) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | scripts/gen_feature_registry.sh (parses features.toml, verifies every test path + suite entry + ci_job, renders the registry) | scripts/gen_feature_registry.sh | — |
| `first-class-references` — First-class safe references (&T / &mut T / &&T in type position) — the compiler rejects the construct (E106); excluded from the release surface | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_type -> diag_safe_ref_not_first_class (E106) | tests/canary_neg/canary_neg_ref_pattern.tg; tests/canary_neg/canary_neg_ref_return.tg; tests/canary_neg/canary_neg_ref_generic.tg | — |
| `fixed-array-const-sizes` — Fixed-array const sizes (literals, const references, constant arithmetic) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/types.tg eval_const_size_expr | tests/canary/canary_pos_fixed_array_const_size.tg | — |
| `gpu-platform` — GPU platforms — the driver-side GpuBackend abstraction (the device enumeration, the buffer create/upload/download path, the shader compilation, the dispatch) and the SOFTWARE/REFERENCE backend (the CPU evaluation of the compute kernels — the implementable one: the software device enumeration, the context, the buffers with the mapped host pointer, the opcode-parse kernel compile (ComputeKernel::create), the elementwise CPU dispatch (set_buffer + dispatch), the shader-compile fail-closed path); the hardware backends (Vulkan/Metal/CUDA/OpenCL) probe the REAL library presence (dynload) and fail closed when the library is absent — never a fabricated device | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | std/gpu.tg (the GpuBackend abstraction + the software/reference backend: enumerate_software_devices / software_device / create_software_context / create_software_buffer / ComputeKernel::create\|set_buffer\|dispatch / compile_shader + the hardware probes via dynload); std/hal.tg (init_hal + _is_backend_supported / _probe_device_count — the software backend always available, the hardware probes fail closed) | tests/gpu/gpu_software_backend_test.tg; tests/hal/hal_software_backend_test.tg | — |
| `inline-assembly` — Inline assembly — no asm directive in token/ast/parser; excluded from the release surface | **implemented + unverified** | `DECLARED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | no asm directive in token/ast/parser | — | — |
| `kernel-primitives` — Kernel primitives (std/kernel.tg) — the real implementations (the alpha stubs completed): the round-robin scheduler table (Scheduler::tick — the slice expiry + the rotation), the CAS-backed mutex (the __intrinsic_atomic_compare_exchange_u32 spin with the acquisition counter), the counting semaphore (acquire / try_acquire / release), the process control block table (register / lookup / set_state over the fixed PCB table) and the host process spawn bridge (spawn_process — the PCB registration + the std::process Command delegation) | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | std/kernel.tg (Scheduler::tick the round-robin table, Mutex::lock the CAS spin, Semaphore::acquire/release, the PROCESS_TABLE registry + register_process / lookup_process / set_process_state + spawn_process) | tests/kernel/kernel_primitives_test.tg | — |
| `kernel-std-behavior` — Kernel-closure std behavior (14 modules) — the kernel itself is the committed workload | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin | bootstrap/compiler_kernel.manifest; the ladder harness compiles the kernel at every stage | scripts/bootstrap_helpers.sh | — |
| `legacy-parameter-spellings` — Legacy parameter spellings (mut/&/&mut/move/own prefixes, x: &T markers, fn(&T) conventions, &self receivers) — the compiler rejects them (E100); excluded from the release surface | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_param / parse_fn_type_param (E100 — legacy parameter spelling; token consumed only for error recovery) | scripts/run_selfhost_grammar_gate.sh | — |
| `lifetimes` — Lifetimes ('static, 'a) — no lifetime tokens exist in the dialect; excluded from the release surface | **implemented + unverified** | `DECLARED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | lexer/parser — no lifetime token exists | — | — |
| `linting` — Linting (tg lint) — the full linter: nine built-in passes over the parsed AST with the TYPED semantic data (the resolver's DefId reachability + the item-index -> DefId table — the LintContext's typed path), the #[allow] suppression + the --allow / --deny / --warn config, the human + --json output, the exit codes (0 clean / 1 errors / 2 warnings-only), and the --list catalog | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/linter.tg lint_source / LintContext / lint_runner_new (the nine passes: unused_variables, unused_imports, dead_code, needless_mut, naming_conventions, unsafe_usage, large_function, redundant_return, empty_block); tg_compiler/driver.tg cmd_lint | tests/lint_suite_test.tg; tests/run_lint_tests.sh | — |
| `lsp` — LSP server (tg lsp) — the JSON-RPC 3.17 server over stdio: initialize / didOpen / didChange diagnostics / completion / hover / definition / references / rename / signatureHelp / formatting / shutdown / exit, the negotiated UTF-16/UTF-8/UTF-32 position units (the astral-scalar offsets), the compiler's own lexer/parser/diagnostics/symbol table consumed at every endpoint, and the client-side incremental document model (std::lsp) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/driver.tg lsp_main_loop / handle_lsp_request / lsp_did_open / lsp_did_change / lsp_hover / lsp_completion / lsp_goto_definition / lsp_references / lsp_rename / lsp_format / lsp_position_to_offset | tests/lsp_delegation_test.tg; tests/run_lsp_tests.sh | — |
| `macros` — Macros (macro keyword, E105 expansion fixpoint) | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg macro machinery (64-pass expansion fixpoint, E105 hard stop) | — | — |
| `mir-verifier` — MIR verifier (CFG/dataflow + projection-aware moved-state lattice) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mir.tg verify_function_v2 (post-lower / post-mono / post-opt) | tests/verifier_projection_tests.tg | `verifier-projection` |
| `modules-visibility` — Modules & visibility (module table = identity authority) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/ast.tg Crate module table; tg_compiler/types.tg crate_module_path_of | scripts/verify_invariants.sh | — |
| `move-consume-semantics` — Move/consume semantics (sink, resource ownership, capability linearity) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/resource_check.tg validate_capability_exit + resource dataflow; tg_compiler/types.tg | tests/canary/canary_resource.tg; tests/canary_neg/canary_neg_resource_use_after_consume.tg; tests/arc_lifecycle_test.tg | — |
| `no-std` — @no_std — the core/std separation + the runtime/link contract: the core-only import surface (std::core / std::ffi / std::embedded / std::atomic — the collection/string/alloc and the OS runtime modules are excluded from the program's import surface), the program's own `def _start() -> !` bare entry (no libc/CRT main wrapper — a `main` is rejected), the direct syscall surface (std::core's __intrinsic_syscall* + the runtime's _exit), and the link contract (the internal linker resolves the entry through the default -> _start -> _reset_handler bare-entry fallback chain; the produced image is fully self-contained) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/driver.tg compile_to_nostd_route (nostd_mode_requested — the --no-std flag or the @no_std attribute; nostd_surface_error — the core-only-surface check; start_entry_error — the _start/! entry + the main rejection); tg_compiler/linker.tg linker_resolve_entry_name (the default -> _start -> _reset_handler bare-entry fallback chain) | tests/nostd/nostd_bare_start.tg; tests/nostd/nostd_core_surface_negative.tg; tests/run_nostd_bare_lane.sh | — |
| `non-kernel-stdlib-behavior` — Non-kernel stdlib behavior — the real behavior suites over the parse-clean modules' pure OS-independent public surfaces (semver parse/satisfies/max_satisfying, the url percent encode/decode, the csv parse, the unicode char classes, the hash determinism) — the parse-clean modules' @test suites | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tests/nonkernel_stdlib_behavior_test.tg (the @test suites over the parse-clean modules' pure surfaces) | tests/nonkernel_stdlib_behavior_test.tg | — |
| `packed-align-attributes` — @repr(C) / @align(N) / @packed layout attributes — the attributed layout rules end-to-end: C-repr structs (the natural C layout + the C-ABI aggregate classification), align-N (the declared alignment, the max-rule field offsets, the tail padding), packed (the packed offsets, align 1, the exact byte span — the kernel epoll_event 12-byte shape) with the byte-wise unaligned load/store lowering and the nested packed aggregates | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg extract_layout_attrs (E109 — the arg shape + the C/packed and packed/align exclusivity); tg_compiler/types.tg validate_layout_attrs (the alignment-overflow bound) + the typed type_layout_attrs table; tg_compiler/layout_engine.tg compute_struct_layout / compute_enum_layout attributed rules + c_aggregate_category (the C-ABI classification); tg_compiler/codegen.tg emit_load_mem_sized_unaligned / emit_store_mem_sized_unaligned + the place_final_static_offset unaligned decision | tests/layout/packed_align_test.tg; tests/layout/packed_align_engine_test.tg; tests/layout/differential_layout_test.tg | — |
| `partial-moves` — Partial moves (projection-aware place-key lattice + masked drop glue) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/types.tg record_place_moves / place_move_states; tg_compiler/mir.tg mir_emit_partial_drop_chain | tests/verifier_projection_tests.tg; tests/canary/canary_pos_resource_partial_extract.tg; tests/canary_neg/canary_neg_resource_partial_double_consume.tg | `verifier-projection` |
| `pkg-manager` — Package manager (tg dep / tg install) — the deterministic SemVer solver (highest-eligible selection with the pre-release policy + the cycle guard), the lockfile integrity verification (schema version / duplicates / the checksum precondition), the cryptographic package verification (the SHA-256 package-tree digest on the miss AND the cache-hit revalidation), the traversal protection (the tar entry-name + link-target validation BEFORE any extraction), the offline reproducibility (the sorted deterministic lockfile + the verified cache reuse) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/pkg_manager.tg Resolver / Lockfile.verify / ensure_cached / hash_package_tree / validate_tar_gz_archive / _extract_tar_gz; tg_compiler/driver.tg cmd_dep / cmd_install | tests/pkg_resolver_test.tg; tests/pkg_hardening_test.tg | — |
| `platform-only-surfaces` — Platform-only surfaces (android / ios / cocoa / windows / gui) — the REAL FFI bindings + the smoke tests: cocoa's ObjC round trip (get_nsstring / nsstring_to_string through objc_getClass / sel_registerName / objc_msgSend — the Mach-O import stubs), windows' wide-string bridge (to_wide_string) + the user32/kernel32 extern sets + the version surface, android's JNI surface (get_device_density + the JNI externs), ios' platform helpers (is_ios_device / get_ios_device_name), and gui's rendering backends (the software CanvasBuffer rasterizer — the CPU clear / put_pixel / fill_rect / frame_rect / blit path the widget surface lowers to when no native backend is attached) | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | std/cocoa.tg get_nsstring / nsstring_to_string (the ObjC round trip); std/windows.tg to_wide_string + the user32/kernel32 externs + get_windows_version; std/android.tg get_device_density + the JNI externs; std/ios.tg is_ios_device / get_ios_device_name; std/gui.tg CanvasBuffer (the software rasterizer) | tests/platform/cocoa_ffi_smoke_test.tg; tests/platform/platform_surface_smoke_test.tg; tests/gui/gui_software_canvas_test.tg | — |
| `progressive-strictness` — Progressive strictness (4-mode config: Dev → Strict → Production → Hardened) — ModeConfig carries ONLY mode + the two unconditional enforcements; the reduced surface IS the completed design (the former configuration bits claimed semantics no pass implements and were deleted, not pending) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mode.tg ModeConfig (mode + enforce_contracts + enforce_capabilities only); the contract check lowering (mir.tg lower_contract) and the capability exit machinery (resource_check.tg validate_capability_exit) enforce unconditionally | tests/run_mode_behavior_tests.sh | — |
| `raw-pointers` — Raw pointers (*T / *mut T, Ptr[T], PtrMut[T]) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg parse_type (* pointer forms); tg_compiler/types.tg typing | tests/run_stdlib_e106_sweep.sh | — |
| `real-time-wcet` — @[real_time(wcet_us=N)] — the analyzer: the annotation parsed and validated (the positive integer bound), the body's static worst-case cost estimated (the operation cost model, the callee costs transitively, the loop bounds via the count annotations), and the over-bound body rejected with the hard validity error | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | parse_real_time_attr + estimate_wcet_cost in tg_compiler/types.tg | tests/wcet_analyzer_test.tg | — |
| `ref-patterns` — Ref patterns (ref x binders) — the compiler rejects them (E106); excluded from the release surface | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/parser.tg diag_ref_pattern_not_supported (E106); binders are never erased | tests/canary_neg/canary_neg_ref_pattern.tg | — |
| `repl` — REPL (tg repl) — the bounded REPL: every entry compiles AND runs through the driver's real machinery as its own process (parse -> type-check -> resource check -> MIR -> codegen -> link -> execute); the multi-line buffering (the block-balance completion check), the stateful session (history, entry numbers), and the runtime-error state preservation (a failed entry never leaks into the next — the buffer clears after every completed entry); expression entries print `=> <code>` | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/driver.tg cmd_repl / repl_eval_source / repl_buffer_complete; tg_compiler/lib.tg Repl (the bounded session struct) | tests/repl_contract_test.tg; tests/run_tool_contract_tests.sh | — |
| `self-host-grammar-gate` — Self-host grammar gate (forbidden legacy forms over the manifest closure, before stage0) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin | scripts/run_selfhost_grammar_gate.sh (enumerate the manifest closure + structural scan + compiler check when probe-validated); wired into run_bootstrap.sh | scripts/run_selfhost_grammar_gate.sh | — |
| `simd` — SIMD intrinsics — the real vector type (the std Vec2/Vec4/Vec8/Vec16 family — the VECTOR ROW: N lanes of the lane type, alignment = the vector width) and the compiler-inline __intrinsic_simd_<op>_<lane> family (add/sub/mul/and/or/xor/load/store/shuffle per the lane widths) lowered by the aarch64 NEON + x86 SSE/AVX codegen arms; the ABI's vector cases (the 16-byte vector through the registers, the 32/64-byte forms by address); the runtime target-feature detection (detect_simd — NEON mandatory on aarch64, SSE2 the x86-64 baseline — the documented contract); the std/simd module's wider surface remains gated experimental; THE HONEST PROOF STATE (P1.12): the intrinsics, the codegen arms and the vector-row layout ARE implemented, and the NATIVE EXACT-VECTOR execution tests are NOT yet run (the tests/simd/ suite executes on the host; the machine-vector equality is unobserved at this SHA) | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/codegen.tg codegen_simd_intrinsic (the NEON + SSE/AVX arms) + the SIMD gate in codegen_intrinsic; tg_compiler/types.tg SimdIntrinsicKind / SimdLane / simd_intrinsic_kind_of (the IntrinsicId domain's SIMD sub-domain); tg_compiler/layout_engine.tg compute_vector_layout / is_simd_vector_type (the vector row) + classify_value_category's vector cases; tg_compiler/codegen.tg AbiArg::Vector / category_uses_sret; std/simd.tg the __intrinsic_simd_* externs + detect_simd (the feature-detection contract) | tests/simd/simd_behavior_test.tg; tests/simd/simd_layout_test.tg; tests/simd/simd_abi_probe.tg; tests/run_simd_tests.sh | `stdlib-new-modules` |
| `status-evidence` — The STATUS evidence document (repository SHA, stage hashes, phase fingerprints, canary counts, target suites, std counts, ABI/integration suites, timestamp, workflow identity — generated from the release run) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | scripts/gen_status.sh (reads the canary manifests' self-describing counts/hashes); the CI status job uploads the status-snapshot artifact | scripts/gen_status.sh | — |
| `stdlib-format-honesty` — The std facade honesty closures (P0.3 + P1.6–P1.9): std::sql PreparedStatement::execute is the explicit Err(Unsupported) — never the silent Ok(empty) result set; std::json parse_document requires the end of input (RFC 8259 — trailing data, the second value and the leading-zero prefix are rejected); std::cbor implements the RFC 8949 indefinite-length forms (additional info 31 + the 0xFF break) with the Appendix A vectors; std::rand's Xoshiro256++ emits the reference rotl(s0 + s3, 23) + s0 formula with the published vectors for every exported PRNG; std::graph's parser is strict (an unknown non-empty line is an error) with the versioned serialization and the round-trip/hash proofs | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/sql.tg PreparedStatement::execute (no Ok(empty)); std/json.tg parse_document (value + whitespace + required EOF); std/cbor.tg the indefinite-length decode/encode + cbor_value_eq; std/rand.tg next_u64 = rotl(s0 + s3, 23) + s0 + XOSHIRO256PP_REF_VECTORS / CHACHA20_ZERO_BLOCK / PCG32_REF_VECTORS; std/graph.tg the versioned GRAPH-1 format + the strict line parser + serialize_graph/save_graph | tests/unit/test_sql_rigor.tg; tests/unit/test_json_rigor.tg; tests/unit/test_cbor_rigor.tg; tests/unit/test_rand_behavior.tg; tests/unit/test_graph_rigor.tg | `stdlib-integration` |
| `stdlib-parse-clean` — Stdlib parse-clean state (all 133 std/*.tg modules) — two-layer sweep | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tests/run_stdlib_e106_sweep.sh (tg check zero diagnostics + the forbidden-syntax grep backstop) | tests/run_stdlib_e106_sweep.sh | `stdlib-e106-sweep` |
| `structured-diagnostics` — Structured diagnostics at the API boundary (Result[AnalyzedProgram, Vec[Diagnostic]]) | **implemented + test-covered** | `SEMANTICALLY_CHECKED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/compiler_core.tg analyze_program / analyze_parsed / analyze_source; stringification only at the CLI boundary | scripts/verify_invariants.sh | — |
| `sync-concurrency` — std::sync / std::thread — the heap-stable Arc-clone guard design, the two-sided channel ownership protocol, Once CAS, Condvar waiter queue, TLS drop, result-cell ownership | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/sync.tg (Arc / Mutex / RwLock / Condvar / Once / atomics); std/thread.tg (spawn / ThreadBuilder / channel / channel_bounded / Once / Barrier) | tests/sync_contention_test.tg; tests/condvar_waiter_queue_test.tg; tests/once_cas_test.tg; tests/thread_channel_ownership_test.tg; tests/thread_local_drop_test.tg; tests/thread_result_cell_test.tg; tests/thread_spawn_failure_test.tg | `stdlib-integration` |
| `syntax-highlighting` — Syntax highlighting — TextMate grammar present, no test evidence | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tangerine-vscode/syntaxes/tangerine.tmLanguage.json (the TextMate grammar) | — | — |
| `test-runner-canonicalization` — @test runner canonicalization (per-test dispatch main; each @test runs as its own process) + the restored CI lanes | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | tg_compiler/driver.tg compile_and_run_tests; the stdlib/gfx-ui lanes (check -> object -> link/import smoke -> native) | tests/run_test_runner_integrity.sh | `stdlib-new-modules` |
| `test-runner-integrity` — tg test outcome integrity (pass / fail / zero-tests / parse-error / implicit-skip) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | tg_compiler/driver.tg cmd_test; P0 gate tests/run_test_runner_integrity.sh | tests/run_test_runner_integrity.sh | `stdlib-new-modules` |
| `tls` — std::tls consuming sink builders + the native shim lane (every declared tls_* extern gated as an exported Mach-O symbol) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin | std/tls.tg sink self: TlsConfig builders; native/tls_shims.c; the CI lane preloads libtg_tls_shims.dylib | tests/tls_interop_test.tg; tests/tls_handshake_test.tg | `stdlib-integration` |
| `traits-generics-bounds` — Traits, generics, bounds | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/trait_resolve.tg; tg_compiler/types.tg bounds checking | tests/canary/canary_pos_generic_policy_instances.tg | — |
| `view-types` — View types (StrView, FfiStr, FfiSlice[T], Slice[T], SliceMut[T]) | **implemented + test-covered** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | std/core.tg StrView; std/ffi.tg FfiStr / FfiSlice[T]; std/collections.tg Slice[T] / SliceMut[T] | tests/run_stdlib_e106_sweep.sh | — |
| `vscode-extension` — VS Code extension — tree present, no test evidence | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tangerine-vscode/package.json + tangerine-vscode/README.md + tangerine-vscode/syntaxes/tangerine.tmLanguage.json (tree present) | — | — |
| `wasi` — WASI (preview1) — the std::wasi guest surface (the Errno / Iovec / ClockId ABI types + the fd_read / fd_write / fd_close / clock_time_get / proc_exit / args / environ externs + the ABI wrappers), the wasm-target import wiring (add_wasi_imports emits the wasi_snapshot_preview1 import section for --target wasm32-wasi), the host-side ABI (the runtime's _tg_wasi_fd_write / _tg_wasi_fd_read / _tg_wasi_fd_close / _tg_wasi_clock_time_get / _tg_wasi_proc_exit implementations — the iovec walk, the errno normalization, the clock_gettime-backed clock), and the conformance (the wasmtime --dir lane runs when the runtime is installed) | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/wasm_target.tg add_wasi_imports (the wasi_snapshot_preview1 import section); tg_compiler/runtime.tg emit_wasi_host_runtime (the _tg_wasi_fd_write / _tg_wasi_fd_read / _tg_wasi_fd_close / _tg_wasi_clock_time_get / _tg_wasi_proc_exit bodies); std/wasi.tg (the Errno / Iovec / ClockId surface + the ABI wrappers) | tests/wasi/wasi_guest_canary.tg; tests/wasi/wasi_guest_surface_test.tg; tests/run_wasi_conformance.sh | — |
| `wasm-target` — WASM compile target — the driver route (--target wasm32-unknown-unknown / wasm32-wasi → the wasm backend), the type/layout mapping (i32/i64/f32/f64/externref per the Tangerine primitives, the linear-memory model), the binary emission (the type/import/function/memory/global/export/start/code/data sections + the producer custom record), and the structural conformance (the emitted binary validated by the independent wasm parser; the wasmtime execution lane runs when the runtime is installed); the std/wasm + std/wasm_js surfaces remain gated experimental | **implemented + test-covered — experimental** | `TARGET_COMPLETE` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/driver.tg is_wasm_target / wasm_config_for_triple / compile_to_wasm_route (the target dispatch); tg_compiler/wasm_target.tg WasmCompiler (the sections + the binary encode) + map_primitive_to_wasm / mir_type_to_wasm / classify_wasm_value_category (the type/layout mapping + the memory model) | tests/wasm/wasm_conformance_test.tg; tests/wasm/wasm_canary.tg; tests/run_wasm_conformance.sh | — |
| `x86-64-target` — x86-64 target (System V / Windows x64) — extern return normalization, Rosetta/qemu lane; codegen/cross-runtime tested, NOT full host-runtime release verified (the native Linux x86-64 host-runtime evidence lands with the linux-x86-64-native CI job's first green run) | **implemented + test-covered** | `TARGET_COMPLETE` | x86_64-unknown-linux-gnu | x64 emitter in tg_compiler/asm.tg / codegen.tg; normalize_extern_return (I32/U32 sign/zero-extension) | tests/run_target_lane_canaries.sh; tests/net_negative_abi_test.tg | `cross-compile` |
| `zero-cost-abstractions` — Zero-cost abstractions (trait dispatch, generics, monomorphization) — no benchmark evidence registered | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mono.tg (zero-inference specialization) | — | — |
| `zero-inference-monomorphization` — Zero-inference monomorphization | **implemented + unverified** | `IMPLEMENTED` | aarch64-apple-darwin, x86_64-unknown-linux-gnu | tg_compiler/mono.tg | — | — |

## Cross-references

- [Feature Matrix](feature_matrix.md) — Tangerine vs Rust; statuses derive from this generated registry.
- [Stdlib Completeness Model](stdlib_completeness.md) — the item-32 verification families + the enumeration gate.
- [Grammar](grammar.md) — the implemented grammar, including the E100/E106 rejection surfaces.
- [Language Reference](language.md) — the current dialect (access conventions, views, FFI pointers).
- [Standard Library Reference](stdlib_reference.md) — module surfaces and the migration-complete gate.
- [Memory Model](memory_model.md) — the normative ownership/access model.
- [Concurrency Guide](concurrency.md) — the access/resource concurrency model (threads, async, shared state).
- `scripts/verify_invariants.sh` — the machine-checkable invariant groups (G1–G12) cited above.
- `features.toml` — the machine-readable source this document is generated from.

---

*Generated by `scripts/gen_feature_registry.sh` from `features.toml` —
 deterministic output, so the CI evidence-gate job can regenerate it and run
 `git diff --exit-code`. Source-state facts only — no feature holds
 implemented+run-verified at SHA and nothing above TARGET_COMPLETE appears
 without `build/release_evidence.json` (the release-run evidence written
 only by `scripts/gen_status.sh --release-evidence`; RELEASE_GATED is
 never derived from source). Do not edit this file by hand.*
