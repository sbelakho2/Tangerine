# Tangerine stdlib contracts manifest — the machine-readable completeness model.
#
# The reviewer item 32 completeness model: every shipped std/*.tg module belongs
# to exactly one verification family, and every family names its MINIMUM PROOF.
# The generator (scripts/gen_stdlib_completeness.sh) renders
# docs/current/stdlib_completeness.md from this file and MECHANICALLY verifies:
#   - the enumeration: every std/*.tg file has a contract entry, and every
#     contract names a real module — an un-contracted module (a new std file)
#     FAILS the gate until it receives a contract + proof tests;
#   - every proof test path exists in the tree;
#   - the family names agree with the completeness model's vocabulary.
#
# The CI enumeration gate lives in tests/run_stdlib_completeness_gate.sh, which
# tests/run_stdlib_e106_sweep.sh (the REQUIRED stdlib-e106-sweep CI job) runs
# before any module is checked.
#
# The reviewer's table enumerated 133 modules; the current tree enumerates the
# count below (computed from std/*.tg, never typed): std/postgres.tg was merged
# into std/db.tg and std/hash_tests.tg was removed in earlier waves. The count
# is derived by the enumeration gate, so any new std file fails the gate until
# it receives a contract + proof tests.

version = 1

[family."kernel"]
minimum_proof = "the bootstrap ladder compiles the module at every stage (bootstrap/compiler_kernel.manifest + run_bootstrap.sh); the E106 sweep keeps it parse-clean"

[family."native"]
minimum_proof = "a committed native behavior suite (tests/unit/*_rigor.tg, tests/*_test.tg) exercises the module's public API via `tg test`"

[family."lane"]
minimum_proof = "a committed CI lane verifies check + object + link/import smoke and the module's own @test suites where declared"

[family."parse-clean"]
minimum_proof = "the E106 sweep only: `tg check` zero-diagnostics + the forbidden-syntax backstop (tests/run_stdlib_e106_sweep.sh)"

[family."experimental"]
minimum_proof = "parse-clean only; the module is flagged experimental (item 33 stable-subset policy) and is EXPLICITLY EXCLUDED from the shipped-std behavior claims"

[module."accessibility"]
family = "native"
proof = ["tests/unit/test_a11y_rigor.tg", "tests/unit/test_a11y.tg"]

[module."alloc"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh", "tests/allocator_churn_test.tg", "tests/allocator_large_test.tg", "tests/allocator_oom_test.tg", "tests/allocator_reuse_test.tg", "tests/allocator_threaded_test.tg", "tests/allocator_alignment_test.tg"]

[module."android"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."anim"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."app"]
family = "native"
proof = ["tests/unit/test_app_rigor.tg", "tests/unit/test_events.tg", "tests/integration/test_pipelines.tg", "tests/determinism/test_replay.tg"]

[module."args"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."assets"]
family = "native"
proof = ["tests/unit/test_assets_rigor.tg"]

[module."async"]
family = "native"
proof = ["tests/unit/test_async_rigor.tg", "tests/task_scope_test.tg", "tests/cancellation_token_test.tg", "tests/reactor_readiness_test.tg", "tests/async_mutex_waiter_test.tg", "tests/async_channel_waiter_test.tg", "tests/async_semaphore_waiter_test.tg", "tests/join_cancel_test.tg", "tests/executor_clock_test.tg", "tests/cancellation_late_child_test.tg", "tests/channel_zero_capacity_test.tg", "tests/deterministic_gate_w_test.tg", "tests/timer_cancelled_root_test.tg", "tests/db_async_pool_test.tg"]

[module."atomic"]
family = "native"
proof = ["tests/atomic_litmus_sb_test.tg", "tests/atomic_litmus_mp_test.tg", "tests/atomic_litmus_wrc_test.tg", "tests/atomic_litmus_acqrel_test.tg", "tests/atomic_litmus_cas_pub_test.tg", "tests/atomic_litmus_cas_fail_test.tg"]

[module."audio"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."audit"]
family = "native"
proof = ["tests/unit/test_audit_rigor.tg"]

[module."auth"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."autotune"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."backend_abi"]
family = "native"
proof = ["tests/abi/test_abi_conformance.tg"]

[module."backtrace"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."bench"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."blas"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."budget"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."capabilities"]
family = "native"
proof = ["tests/unit/test_capability.tg"]

[module."cbor"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."cli"]
family = "native"
proof = ["tests/unit/test_cli_rigor.tg"]

[module."cocoa"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."collections"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."compositor"]
family = "native"
proof = ["tests/unit/test_compositor_rigor.tg"]

[module."compress"]
family = "native"
proof = ["tests/unit/test_compress_rigor.tg"]

[module."config"]
family = "native"
proof = ["tests/unit/test_config_rigor.tg"]

[module."contracts"]
family = "native"
proof = ["tests/unit/test_contracts_rigor.tg", "tests/run_mode_behavior_tests.sh"]

[module."core"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."crypto"]
family = "native"
proof = ["tests/unit/test_crypto_rigor.tg"]

[module."csv"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."ctx"]
family = "native"
proof = ["tests/unit/test_ctx_rigor.tg"]

[module."db"]
family = "native"
proof = ["tests/db_integration_test.tg", "tests/db_lifecycle_test.tg", "tests/db_async_pool_test.tg", "tests/db_postgres_integration_test.tg", "tests/db_mysql_integration_test.tg", "tests/db_postgres_lexer_test.tg", "tests/db_mysql_layout_probe_test.tg", "tests/db_mysql_large_result_test.tg", "tests/db_pool_capacity_test.tg", "tests/db_pool_init_failure_test.tg"]

[module."debug"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."device"]
family = "native"
proof = ["tests/unit/test_device_rigor.tg"]

[module."diagnostics"]
family = "native"
proof = ["tests/unit/test_diag_rigor.tg"]

[module."doc_gen"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."dynload"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."effects"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."embed_trace"]
family = "native"
proof = ["tests/unit/test_embed_trace_rigor.tg"]

[module."embedded"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."encoding"]
family = "native"
proof = ["tests/unit/test_encoding_rigor.tg"]

[module."env"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."exec"]
family = "native"
proof = ["tests/exec_conservation_test.tg", "tests/exec_admission_freeze_test.tg", "tests/exec_global_executor_test.tg", "tests/exec_par_family_test.tg", "tests/exec_victim_range_test.tg"]

[module."ffi"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."fft"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."float_control"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh", "std/float_control.tg"]

[module."fmt"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."fs"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."fuzz"]
family = "native"
proof = ["tests/unit/test_fuzz_rigor.tg", "tests/fuzz/test_fuzz.tg"]

[module."geom"]
family = "native"
proof = ["tests/unit/test_geom.tg", "tests/unit/test_paint.tg", "tests/consistency/test_consistency.tg"]

[module."gfx"]
family = "native"
proof = ["tests/unit/test_gfx_rigor.tg", "tests/unit/test_paint.tg", "tests/integration/test_pipelines.tg", "tests/consistency/test_consistency.tg", "tests/golden/test_visual_regression.tg"]

[module."gfx_errors"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."gfx_gpu"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]
note = "committed host-side rigor artifact(s) exist but are non-claiming for the unserved platform target"

[module."gpu"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."gpu_metal"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."gpu_vulkan"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."gpu_webgpu"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."graph"]
family = "native"
proof = ["tests/unit/test_graph_rigor.tg"]

[module."gui"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."hal"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]
note = "committed host-side rigor artifact(s) exist but are non-claiming for the unserved platform target"

[module."hash"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."http"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."http2"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."i18n"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."image"]
family = "native"
proof = ["tests/integration/test_pipelines.tg", "tests/consistency/test_consistency.tg", "tests/determinism/test_replay.tg"]

[module."input"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."io"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."ios"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."json"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."kernel"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."linalg"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh", "std/linalg.tg"]

[module."lint"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."locale"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."log"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."lsp"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."math"]
family = "native"
proof = ["tests/unit/test_math_rigor.tg"]

[module."metrics"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."migrate"]
family = "native"
proof = ["tests/unit/test_migrate_rigor.tg"]

[module."mmap"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."msgpack"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."net"]
family = "native"
proof = ["tests/net_loopback_test.tg", "tests/net_negative_abi_test.tg"]

[module."obligations"]
family = "native"
proof = ["tests/unit/test_obligations_rigor.tg"]

[module."opentelemetry"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh", "std/opentelemetry.tg"]

[module."package"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."patch"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."path"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."perf"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."platform"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."process"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh"]

[module."profile"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."rand"]
family = "native"
proof = ["tests/rand_entropy_test.tg"]

[module."random"]
family = "native"
proof = ["tests/unit/test_random_rigor.tg"]

[module."regex"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."replay"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."secure_types"]
family = "native"
proof = ["tests/unit/test_secure_types_rigor.tg"]

[module."semantic_diff"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."semver"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."serde"]
family = "native"
proof = ["tests/unit/test_serde_rigor.tg"]

[module."signal"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."simd"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."snapshot"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."sql"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."sqlite"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."stats"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."supply_chain"]
family = "native"
proof = ["tests/unit/test_supply_chain_rigor.tg"]

[module."sync"]
family = "native"
proof = ["tests/unit/test_sync_rigor.tg", "tests/sync_contention_test.tg", "tests/condvar_waiter_queue_test.tg", "tests/once_cas_test.tg", "tests/arc_lifecycle_test.tg", "tests/thread_channel_ownership_test.tg"]

[module."taint"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh", "tests/unit/test_taint_rigor.tg"]

[module."tensor"]
family = "native"
proof = ["tests/unit/test_tensor_rigor.tg"]

[module."term"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."test"]
family = "native"
proof = ["tests/unit/test_test_rigor.tg", "tests/run_test_runner_integrity.sh", "tests/run_bench_runner_integrity.sh"]

[module."test_gen"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."text"]
family = "native"
proof = ["tests/unit/test_text_rigor.tg", "tests/unit/test_text.tg"]

[module."thread"]
family = "native"
proof = ["tests/thread_channel_ownership_test.tg", "tests/thread_local_drop_test.tg", "tests/thread_result_cell_test.tg", "tests/thread_spawn_failure_test.tg", "tests/pthread_abi_test.tg"]

[module."time"]
family = "kernel"
proof = ["bootstrap/compiler_kernel.manifest", "tests/run_stdlib_e106_sweep.sh", "tests/timer_cancel_test.tg", "tests/timer_cancelled_root_test.tg", "tests/executor_clock_test.tg"]

[module."tls"]
family = "native"
proof = ["tests/unit/test_tls_rigor.tg", "tests/tls_interop_test.tg", "tests/tls_handshake_test.tg"]

[module."toml"]
family = "native"
proof = ["tests/unit/test_toml_rigor.tg"]

[module."tui"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."ui"]
family = "native"
proof = ["tests/unit/test_ui_rigor.tg"]

[module."ui_toolkit"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."unicode"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."url"]
family = "native"
proof = ["tests/unit/test_url_rigor.tg"]

[module."validation"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh", "std/validation.tg"]

[module."wasm"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]
note = "committed host-side rigor artifact(s) exist but are non-claiming for the unserved platform target"

[module."wasm_js"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]
note = "host-side @test suite (CI stdlib-new-modules lane); the wasm32/js target itself is unserved"

[module."web"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."web_ext"]
family = "lane"
proof = ["tests/run_stdlib_e106_sweep.sh", "std/web_ext.tg"]

[module."web_server"]
family = "native"
proof = ["tests/unit/test_web_server_rigor.tg"]

[module."websocket"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."window"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."windows"]
family = "experimental"
proof = ["tests/run_stdlib_e106_sweep.sh"]

[module."yaml"]
family = "parse-clean"
proof = ["tests/run_stdlib_e106_sweep.sh"]

