enum Color
  Red
  Green
  Blue
end

def is_green(c: Color) -> Bool
  c == Color::Green
end

def main() -> Int
  var s = 0
  if is_green(Color::Green) then s = s + 1 end
  if is_green(Color::Blue) then s = s + 2 end
  s
end
