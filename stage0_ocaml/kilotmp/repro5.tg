enum K
  A(Int)
  B(Int, Int)
  C(Int)
end

def f(k: K, x: Int) -> Int
  match k
  when K::A(a) then
    a
  when K::B(b, c) then
    b + c + x
  when K::C(a) then
    a + x
  end
end

def main() -> Int
  f(K::B(3, 4), 100)
end
