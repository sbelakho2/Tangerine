STATUS: 2026-08-20 — snapshot at commit 5f6e56fb7732cf9f37ebc2440b5e06c3c4c87a38
(HEAD of main, working tree with uncommitted consolidation changes:
 tg_compiler/{access_check,asm,ast,bindgen,cap_baseline,codegen,
 compiler_core,docgen,driver,formatter,ids,layout_engine,lexer,lib,
 linker,linter,mir,mono,object,parser,pkg_manager,registry,resolver,
 resource_check,runtime,token,trait_resolve,types,wasm_target}.tg
 + std/{alloc,backend_abi,backtrace,cli,collections,contracts,core,
 embedded,ffi,fmt,fs,http,lint,package,profile,regex,secure_types,
 snapshot,text,url,wasm}.tg
 + tests/layout_tests.tg + tests/canary_neg/MANIFEST
 + this snapshot (docs/current/*, STATUS.txt, tests/*))

SNAPSHOT NOTICE — counts not auto-generated for this snapshot:
  No bootstrap/test run was executed while producing this file. The
  previously reported counts ("465 passed / 0 failed; parse 438/443")
  could not be reproduced from the repository (no recorded log, and the
  Swift TangerineTestRunner does not produce them on this tree). Treat
  any numeric test count as unverified until regenerated.

CURRENT STATE (structural, verified against the working tree):
- Stage0 (Swift interpreter) exists; the ladder is run via ./run_bootstrap.sh
  (stage0 -> stage1 -> stage2 -> stage3 with stage2 == stage3 byte-identical
  and per-phase fingerprints; --skip-ladder/--skip-determinism options exist
  but a full run is the release gate).
- Bootstrap closure: bootstrap/compiler_kernel.manifest, 37 sources
  (14 std + 23 compiler files); build/bootstrap-input.json records the
  aggregate source hash 31a0d6fa... (aarch64-apple-darwin; regenerated by
  the manifest-hash tool against the current working tree).
- Native compiler pipeline: lex -> parse (E106 hard error on first-class
  `&T` type positions; the extern-declaration ABI parser
  parse_extern_abi_type is the ONE allowed reference position) ->
  dependency merge -> macro expansion (E105 fixpoint) -> resolve (strict,
  forced) -> type check (typed HIR) -> access check -> resource check
  (capabilities enforced) -> MIR (verify_mir post-lower/mono/opt) -> mono
  (zero-inference) -> codegen (aarch64 host; x86-64 cross lane) -> link.
- Canary suites (tests/canary, tests/canary_neg, tests/arm64) hold the
  three-way parity (declared count == listed == discovered) with 75, 53
  and 2 files respectively; the acceptance-test additions below were
  manifest-registered but NOT yet executed (no compiler run this snapshot).
- Known in-progress work (working tree): the E106 migration of the
  non-kernel stdlib modules (see docs/current/stdlib_reference.md
  "Completeness Status"); std/collections.tg record-visit extern
  signatures still carry Option[&K]/&V return positions (the kernel's
  last reference positions, parsed through the extern-ABI exception).

TO REGENERATE THIS FILE (generated-by instructions):
  1. Run the full harness:  ./run_bootstrap.sh            # ladder + determinism
  2. Run the native suites:  ./tests/run_conformance_canaries.sh <build/tg_stage3> <outdir>
                             ./tests/run_target_lane_canaries.sh <build/tg_stage3> <outdir> [triple]
                             ./tests/run_stdlib_e106_sweep.sh <build/tg_stage3> <outdir>
                             ./tests/run_mode_behavior_tests.sh <build/tg_stage3> <outdir>
  3. Replace this snapshot block with the summary lines printed by the
     harness (per-suite "N/M passed" counts and the stage2 == stage3
     verdict), the commit SHA at the time of the run, and today's date.
  4. Keep the SNAPSHOT NOTICE only if the counts were not produced by a
     run; delete it when a real run's numbers replace them.

HARNESS CONSTANT PENDING (must be bumped with the manifest counts):
  scripts/bootstrap_helpers.sh CANARY_SUITE_POSITIVE_COUNT=66 -> 75 and
  CANARY_SUITE_NEGATIVE_COUNT=52 -> 53 (tests/canary/MANIFEST and
  tests/canary_neg/MANIFEST already declare 75/53 and hold parity). The
  harness's four-way check (declared == listed == discovered == constant)
  will fail until the constants are updated together with the manifests.

KNOWN ISSUES RECORDED BY THE 2026-08-20 AUDIT (verification only; fixes
outside the docs/STATUS/tests edit scope are tracked here):
  - tg_compiler/mir.tg:20235 still carries the comment token
    "Reviewer item 100 +:" — the comment sweep's reviewer-item token was
    missed; the surrounding text describes the embedded-types concreteness
    walk and should keep its content, only the "Reviewer item 100 +:"
    prefix should be dropped.
  - The std declares no `impl Clone for String` and no `impl Eq for
    String` (nor Clone/Eq for the primitives): `String.clone()` resolves
    through the builtin method table (resolve_method_type's String arm ->
    the StringClone intrinsic), but the Clone/Eq TRAIT bounds are not
    satisfiable for String (the obligation solver has only the Copy
    fallback). docs/current/memory_model.md and std/collections.tg call
    String "Clone, NOT Copy" — the method exists, the trait impl does not;
    any `[T: Clone]`/`[T: Eq]` generic instantiated with String fails to
    solve. The new canaries exercise the Clone-bounded collection surface
    (entries_cloned / set algebra) through the ungated method-resolution
    path; adding `impl Clone for String` (and Eq) in std/core.tg would
    make the bounds genuinely satisfiable.
  - tests/layout_tests.tg, tests/canary/* and tests/canary_neg/* were
    verified structurally (block balance, manifest parity); no compiler
    run was available to execute them in this snapshot (the checked-in
    tg_stage1_v2x binaries predate the round-6 std changes and crash on
    the current tree).
