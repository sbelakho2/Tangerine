STATUS: 2026-07-21

Stage0 (Swift interpreter) → Stage1 (native) bootstraps successfully.
Stage1 crashes with SIGSEGV on first compilation attempt.

ROOT CAUSE IDENTIFIED:
- Enum discriminants in MIR were wrongly mapped: A64::X11 → 10 instead of 11,
  A64::X19 → 10 instead of 19, etc. This caused emitted ARM64 encodings (LDRB/STRB)
  to reference wrong registers.

- After fixing enum discriminants (rebuilding Swift stage0), the register encodings
  are now correct (ldrb w11,[x19] instead of ldrb w10,[x10]).

REMAINING ISSUES:
1. str_slice → _tg_str_slice crash: source pointer invalid (x19 = 0x101034000)
   - maybe_load_string_data_ptr clobbers x1/x2 → fixed by saving to X19/X20

2. str_concat crash at .Lsc_lb: string pointer x20 is NULL (0x48b = tiny address)
   - ldrb X10,X10 → X11,X10 fixed (correct encoding now)
   - But string pointer is still null → need to investigate argument setup

3. _tg_bump_alloc mmap returns non-writable page → cause unknown
   - C test shows mmap works with same args
   - Potential kernel bug or ASLR interaction on macOS ARM64

NEXT STEPS:
- Pre-allocate static buffer for bump_alloc to avoid mmap
- Fix null string concurrency in str_concat
- Trace calling convention for str_slice setup
