# Tangerine Compiler — Main Entry Point
# Self-hosting compiler written in Tangerine
#
# This is the executable entry point that delegates to the driver.

use tg_compiler::driver

def main() -> Int
  driver::driver_main()
end